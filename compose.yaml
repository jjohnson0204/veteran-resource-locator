services:
  postgres:
    image: 'postgres:17'
    environment:
      - POSTGRES_DB=veteran_resources
      - POSTGRES_PASSWORD=secret
      - POSTGRES_USER=veteran_user
    ports:
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: